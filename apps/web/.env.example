NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Phase 1 isolated analytics grid foundation.
# Set NEXT_PUBLIC_ENABLE_NEW_GRID_SYSTEM=false to force legacy-only rendering when the new shell is wired in later.
ENABLE_NEW_GRID_SYSTEM=true
NEXT_PUBLIC_ENABLE_NEW_GRID_SYSTEM=true
